<h1>flats List</h1>

<ul id="flats">
  <% @flats.each do |flat| %>
    <li>
      <%= link_to flat.name, flat_path(flat.id) %>
    </li>
  <% end %>
</ul>

<hr>

<%= link_to new_flat_path do %>
  <i class="fa fa-plus"></i> Add a new flat
<% end %>
